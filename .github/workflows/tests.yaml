name: Run Project Tests

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test_data_classes:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: collection/data_collection

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: collection/data_collection/requirements.txt

      - name: Install dependencies (cached)
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: "[TEST] CITY CLASS"
        run: |
          python -m unittest tests/test_city_class.py

  test_bike_class:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: collection/data_collection

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: collection/data_collection/requirements.txt

      - name: Install dependencies (cached)
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: "[TEST] CLI"
        run: |
          python -m unittest tests/test_bike_class.py

  test_station_class:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: collection/data_collection

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: collection/data_collection/requirements.txt

      - name: Install dependencies (cached)
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: "[TEST] STATION"
        run: |
          python -m unittest tests/test_station_class.py

  test_cli:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: collection/data_collection

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: collection/data_collection/requirements.txt

      - name: Install dependencies (cached)
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: "[TEST] CLI"
        run: |
          python -m unittest tests/test_cli.py

  test_postgres:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: collection/data_collection

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: collection/data_collection/requirements.txt

      - name: Install dependencies (cached)
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: "[TEST] POSTGRES"
        run: |
          python -m unittest tests/test_postgres.py

  test_nextbike_api:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: collection/data_collection

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: collection/data_collection/requirements.txt

      - name: Install dependencies (cached)
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: "[TEST] POSTGRES"
        run: |
          python -m unittest tests/test_nextbikeapi.py
